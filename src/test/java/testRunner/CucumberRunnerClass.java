/*package testRunner;
import io.cucumber.testng.AbstractTestNGCucumberTests;
import io.cucumber.testng.CucumberOptions;
import org.testng.annotations.Test;

@CucumberOptions(
	    features = "src/main/resources/featureFiles", // Path to your feature files
	    glue = "stepDefinition", // Package containing your step definitions
	    plugin = {"pretty", "html:target/cucumber-reports"} // Specify the plugin for HTML reports
	)

public class CucumberRunnerClass extends AbstractTestNGCucumberTests {

}*/
